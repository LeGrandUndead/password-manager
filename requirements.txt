# PyPass — dependency list
# -------------------------
# Core cryptography
cryptography>=42.0.0       # AES-GCM via hazmat layer

# Argon2id KDF (preferred — memory-hard, GPU-resistant)
# If not installable, the application falls back to Scrypt (stdlib)
argon2-cffi>=23.1.0

# No other runtime dependencies — stdlib only for everything else
# (hashlib for scrypt/pbkdf2, os.urandom for CSPRNG, json, getpass, etc.)
